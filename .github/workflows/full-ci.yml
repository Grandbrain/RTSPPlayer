name: Full CI

on: [push, pull_request]

jobs:
  rtspplayerserver-ci:
    
    name: RTSPPlayerServer CI
    runs-on: ${{matrix.platform}}
    
    strategy:
      matrix:
        platform: [ubuntu-latest, windows-latest]
    
    defaults:
      run:
        working-directory: Source/RTSPPlayerServer

    steps:
    - uses: actions/checkout@v2
    - name: Setup .NET Core
      uses: actions/setup-dotnet@v1
      with:
        dotnet-version: 3.1.301
    - name: Install dependencies
      run: dotnet restore
    - name: Build
      run: dotnet build --no-restore
    - name: Test
      run: dotnet test --no-restore --verbosity normal
    - if: matrix.platform == 'ubuntu-latest'
      name: Deploy (Linux x64, self-contained, single file, trimmed)
      run: dotnet publish -r linux-x64 -c Release -p:PublishSingleFile=true -p:PublishTrimmed=true
    - if: matrix.platform == 'windows-latest'
      name: Deploy (Windows x64, self-contained, single file, trimmed)
      run: dotnet publish -r win-x64 -c Release -p:PublishSingleFile=true -p:PublishTrimmed=true
